# This makes Grafana auto-connect to Prometheus on startup, so you don't
# have to manually add it through the UI every time containers restart.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
